{
  "name": "kanbots",
  "version": "0.0.0",
  "private": true,
  "description": "Local collaboration interface for working on GitHub Issues with Claude Code agents",
  "license": "MIT",
  "author": "hello@kanbots.dev",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "desktop": "pnpm --filter @kanbots/desktop launch",
    "desktop:dev": "pnpm --filter @kanbots/desktop dev",
    "release:dry": "pnpm -r publish --access public --dry-run --no-git-checks",
    "release": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.18.0",
    "globals": "^17.5.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "electron",
      "esbuild"
    ]
  }
}
